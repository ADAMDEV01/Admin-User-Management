export function Pagination({ page, pages, onChange }: { page: number; pages: number; onChange: (page: number) => void }) { return <div className="pagination"><span>Page <b>{page}</b> of <b>{pages}</b></span><div><button disabled={page===1} onClick={()=>onChange(page-1)} aria-label="Previous page">‹</button><button disabled={page===pages} onClick={()=>onChange(page+1)} aria-label="Next page">›</button></div></div> }
